---
import '../styles/global.css';
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "关于我";

const identity = {
  name: "蓝染",
  zone: "中国",
  occupation: "后端开发工程师",
  hobbies: ["摄影", "旅游"],
};

const skills = ["Java", "SpringBoot", "Kotlin", "Python", "Rust", "Web3"];

const skillColor = "navy"
const fontWeight = "bold";
const textCase = "uppercase";
---

<BaseLayout pageTitle = {pageTitle}>
      <style define:vars={{skillColor, fontWeight, textCase}}>
          h1 {
              color: purple;
              font-size: 4rem;
          }
          .skill {
              color: var(--skillColor);
              font-weight: var(--fontWeight);
              text-transform: var(--textCase);
          }
      </style>

    <h2>……和我的新 Astro 网站！</h2>

    <p>我正在学习 Astro 的入门教程。这是我网站上的第二个页面，也是我自己建立的第一页面！</p>

    <p>随着我完成更多教程，该站点将更新，所以请继续查看我的旅程将如何进行吧！</p>

    <p>以下是关于我的几个事实：</p>
    <ul>
        <li>我的名字是{identity.name}.</li>
        <li>我住在{identity.zone}。我的职业是{identity.occupation}。</li>
        <li>爱好{identity.hobbies.toString()}</li>
    </ul>
    <p>技能：</p>
    <ul>
        {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>
</BaseLayout>