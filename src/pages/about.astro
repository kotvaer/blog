---
import BaseLayout from "../layouts/BaseLayout.astro";
import { AUTHOR_NAME, CONTACT_EMAIL } from '../utils/constants.js';

const pageTitle = "关于我";

const identity = {
  name: AUTHOR_NAME,
  zone: "中国",
  occupation: "后端开发工程师",
  hobbies: ["摄影", "旅游"],
};

const skills = ["Java", "SpringBoot", "Kotlin", "Python", "Rust", "Web3"];

const avatarUrl = "/images/profile.jpeg"; // 您的头像文件路径
---

<!--<BaseLayout>-->
<BaseLayout>
  <div class="about-page-wrapper">
    <div class="profile-header">
      <img src={avatarUrl} alt="我的头像" class="avatar" />
      <div class="contact-info">
        <a href={`mailto:${CONTACT_EMAIL}`} class="contact-button">
          Contact me
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
            <path d="M24 21h-24v-18h24v18zm-23-16.477v14.477h22v-14.477l-11 8.999-11-8.999zm21.084-2.523l-10.084 8.193-10.084-8.193h20.168z"/>
          </svg>
        </a>
      </div>
      <h2 class="profile-name">你好，我是 {identity.name}！</h2>
      <p class="profile-tagline">一名{identity.occupation}，对技术充满热情。</p>
    </div>

    <div class="content-grid">
      <section class="card intro-section">
        <h3>我的故事</h3>
        <p>我喜欢通过这个博客分享我的知识和经验，记录我的学习旅程。</p>
        <p>随着我完成更多教程，该站点将更新，所以请继续查看我的旅程将如何进行吧！</p>
      </section>

      <section class="card details-section">
        <h3>关于我的几个事实：</h3>
        <ul>
          <li>我的名字是 <span>{identity.name}</span>。</li>
          <li>我住在 <span>{identity.zone}</span>。</li>
          <li>我的职业是 <span>{identity.occupation}</span>。</li>
          <li>爱好 <span>{identity.hobbies.join('、')}</span>。</li>
        </ul>
      </section>

      <section class="card skills-section">
        <h3>我的技能：</h3>
        <div class="skills-grid">
          {skills.map((skill) => <span class="skill-tag">{skill}</span>)}
        </div>
      </section>
    </div>
  </div>
</BaseLayout>

<style>
  .about-page-wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
    padding: 2rem 1rem;
  }

  .profile-header {
    display: flex;
    flex-direction: column;
    align-items: center; /* 确保所有子元素水平居中 */
    width: 100%; /* 确保占据可用宽度 */
    margin-bottom: 2rem;
  }

  .contact-info {
    margin-top: 1.5rem;
    text-align: center;
  }

  .contact-info p {
    font-size: 1.15rem; /* 正文字体大小 */
    color: #555; /* 正文字体颜色 */
    margin-bottom: 0.5rem;
    display: flex; /* 使p标签成为flex容器 */
    align-items: center; /* 垂直居中文本和图标 */
    justify-content: center; /* 水平居中 */
    gap: 0.5rem; /* 文字和图标之间的间距 */
  }

  .contact-text {
    transform: translateY(-2px); /* 微调文字向上移动 */
  }

  .avatar {
    width: 250px;
    height: 250px;
    border-radius: 50%;
    object-fit: cover;
    border: 5px solid #ff9776;
    box-shadow: 0 0 25px rgba(0, 0, 0, 0.4);
    margin-bottom: 1.5rem; /* 保持底部间距 */
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .avatar:hover {
    transform: scale(1.08); /* 悬停时放大更多 */
    box-shadow: 0 0 40px rgba(0, 0, 0, 0.5); /* 悬停时阴影更深 */
  }

  .profile-name {
    font-size: 2.8rem;
    color: #333;
    margin-bottom: 0.5rem;
    text-align: center;
  }

  .profile-tagline {
    font-size: 1.3rem;
    color: #666;
    text-align: center;
  }

  .page-title {
    font-size: 3.5rem;
    color: #333;
    margin-bottom: 1rem;
    text-align: center;
  }

  .content-grid {
    display: grid;
    grid-template-columns: 1fr; /* 始终单列 */
    gap: 2rem;
    max-width: 1000px; /* 限制内容网格的最大宽度 */
    width: 100%;
  }

  .card {
    background-color: #fff;
    padding: 2.5rem;
    border-radius: 12px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    text-align: center;
  }

  .card:hover {
    transform: translateY(-8px); /* 悬停上浮 */
    box-shadow: 0 12px 25px rgba(0, 0, 0, 0.15);
  }

  .card h3 {
    font-size: 2rem;
    color: #ff9776;
    margin-bottom: 1.5rem;
  }

  .intro-section p {
    font-size: 1.15rem;
    line-height: 1.8;
    color: #555;
  }

  .details-section ul {
    list-style: none;
    padding: 0;
  }

  .details-section li {
    font-size: 1.1rem;
    line-height: 2;
    color: #555;
    margin-bottom: 0.5rem;
  }

  .details-section li span {
    font-weight: bold;
    color: #333;
  }

  .skills-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.8rem;
  }

  .skill-tag {
    background-color: #e0e0e0;
    color: #333;
    padding: 0.7rem 1.4rem;
    border-radius: 25px;
    font-size: 1rem;
    font-weight: 600;
    transition: background-color 0.3s ease, color 0.3s ease;
  }

  .skill-tag:hover {
    background-color: #ff9776;
    color: #fff;
  }

  /* Dark Mode Styles */
  html.dark .profile-name, html.dark .profile-tagline {
    color: #e2e8f0;
  }

  html.dark .card {
    background-color: #2d3748;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
  }

  html.dark .card h3 {
    color: #ff9776;
  }

  html.dark .intro-section p, html.dark .details-section li {
    color: #cbd5e0;
  }

  html.dark .details-section li span {
    color: #e2e8f0;
  }

  html.dark .skill-tag {
    background-color: #4a5568;
    color: #e2e8f0;
  }

  html.dark .skill-tag:hover {
    background-color: #ff9776;
    color: #1a202c;
  }

  .contact-info {
    margin-top: 1.5rem;
    text-align: center;
  }

  .contact-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.8rem 1.5rem;
    background-color: #ff9776;
    color: #fff;
    border-radius: 6px;
    text-decoration: none;
    font-weight: bold;
    transition: background-color 0.3s ease, transform 0.2s ease;
  }

  .contact-button:hover {
    background-color: #e07b5e;
    transform: scale(1.05);
  }

  .contact-button svg {
    width: 1.2em;
    height: 1.2em;
    fill: currentColor; /* Make SVG color match text color */
  }

  /* Dark Mode for contact-button */
  html.dark .contact-button {
    background-color: #ff9776;
    color: #1a202c;
  }

  html.dark .contact-button:hover {
    background-color: #e07b5e;
  }
</style>