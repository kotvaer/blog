---
---
<div class="nav-links">
    <a href="/">首页</a>
    <a href="/blog">博客</a>
    <a href="/tags">标签</a>
    <a href="/about">关于</a>
</div>

<style>
    .nav-links {
        display: flex;
        gap: 1.5rem;
    }

    .nav-links a {
        text-decoration: none;
        color: #333;
        font-weight: 500;
        position: relative;
        padding: 0.5rem 1.5rem; /* 调整padding以适应垂直居中 */
        display: flex; /* 使a标签成为flex容器 */
        align-items: center; /* 垂直居中文本 */
        transition: background-color 0.3s ease, color 0.3s ease; /* 添加过渡效果 */
    }

    .nav-links a:hover, .nav-links a:focus {
        background-color: #ff9776; /* 悬停背景色 */
        color: #fff; /* 悬停文字颜色 */
        border-radius: 200px; /* 增大圆角 */
    }

    /* Dark Mode */
    html.dark .nav-links a {
        color: #e2e8f0;
    }

    html.dark .nav-links a:hover, html.dark .nav-links a:focus {
        background-color: #ff9776;
        color: #1a202c; /* 暗黑模式悬停文字颜色 */
    }